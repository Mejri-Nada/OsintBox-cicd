/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "",
  assetPrefix: "",
  images: {
    unoptimized: true,
  },
  //output: "standalone", // or remove output completely
};

export default nextConfig;
